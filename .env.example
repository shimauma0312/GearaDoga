#===============================================================
# MT5接続情報 - MetaTrader 5への接続に必要な設定
#===============================================================
# MT5アカウント番号 - ブローカーから発行されたログイン番号を入力
MT5_ACCOUNT=12345678
# MT5アカウントパスワード - アカウントのログインパスワード
MT5_PASSWORD=your_password
# MT5サーバー名 - 使用するブローカーのサーバー名（ブローカー指定の正確な名前を使用）
MT5_SERVER=your_broker_server

#===============================================================
# 取引設定 - 自動売買の基本パラメータ
#===============================================================
# 取引通貨ペア - 例: USDJPY, EURUSD, GBPJPY, XAUUSD(金)など
TRADING_SYMBOL=USDJPY
# 時間枠 - 以下の定数から選択:
# TIMEFRAME_M1(1分足), TIMEFRAME_M5(5分足), TIMEFRAME_M15(15分足),
# TIMEFRAME_M30(30分足), TIMEFRAME_H1(1時間足), TIMEFRAME_H4(4時間足),
# TIMEFRAME_D1(日足), TIMEFRAME_W1(週足), TIMEFRAME_MN1(月足)
TIMEFRAME=TIMEFRAME_M5
# ロットサイズ - 取引量（ミニロット: 0.01 = 1,000通貨, スタンダードロット: 1.0 = 100,000通貨）
LOT_SIZE=0.01
# ストップロスのpips - 損失を制限するための価格変動幅（JPY通貨ペアでは約0.01円=1pips）
STOP_LOSS_PIPS=20
# テイクプロフィットのpips - 利益確定のための価格変動幅
TAKE_PROFIT_PIPS=30

#===============================================================
# 機械学習モデル設定 - 予測モデルの設定
#===============================================================
# モデルファイルの保存場所 - RandomForestClassifierモデルのパス
MODEL_PATH=models/trading_model.pkl
# スケーラーファイルの保存場所 - 特徴量の標準化パラメータ
SCALER_PATH=models/scaler.pkl
# 学習に使用するデータ期間 - 過去のローソク足本数（多いほど精度が向上する可能性があるが、
# 古すぎるデータは役に立たない場合もある、10,000〜100,000の範囲が一般的）
TRAINING_PERIODS=25000
# モデルの再学習間隔（日数）- 市場環境の変化に適応するため定期的に再学習する間隔
# 1〜30の範囲で設定（短すぎると計算リソースを消費し、長すぎると適応が遅れる）
RETRAIN_DAYS=7

#===============================================================
# 強化学習設定 - DQNによる強化学習の設定
#===============================================================
# 強化学習を使用するかどうか - True/Falseで指定
# True: 従来の機械学習と強化学習の両方を使用
# False: 従来の機械学習モデルのみを使用（パフォーマンスが安定している場合はこちらがおすすめ）
USE_RL=False
# 強化学習の学習のみを常に行うかどうか - True/Falseで指定
# True: USE_RL=Falseでも強化学習モデルは裏で継続的に学習する
# False: USE_RL=Falseの場合は強化学習モデルの学習も行わない
RL_ALWAYS_TRAIN=True
# 強化学習モデルファイルのパス - PyTorchのDQNモデルの保存先
RL_MODEL_PATH=models/dqn_model.pth
# リプレイバッファの保存先 - エージェントの経験を保存するバッファのパス
RL_BUFFER_PATH=models/replay_buffer.pkl
# ターゲットネットワーク更新間隔 - 何回の更新ごとにターゲットネットワークを更新するか
# 50〜500の範囲で設定（小さいほど頻繁に更新されるが、学習が不安定になる可能性がある）
RL_UPDATE_INTERVAL=100
# 学習バッチサイズ - 一度に学習に使用するサンプル数
# 16〜128の範囲で設定（大きいほどメモリ使用量が増えるが学習が安定する）
RL_TRAIN_BATCH_SIZE=32
# 起動時に強化学習モデルを再学習するかどうか - True/Falseで指定
# True: プログラム起動時に過去データで強化学習モデルを再学習する（時間がかかる）
# False: 既存のモデルをそのまま使用する（高速に起動できる）
RL_RETRAIN_ON_START=False
